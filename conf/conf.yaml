im_balance:
  ws:
    addr: localhost:18080
  grpc:
    addr: localhost:8081
    heart_beat:
      interval: 10

im_manage:
  ws:
    addr: localhost:18082
    heart_beat:
      interval: 10

im_api:
  http:
    addr: localhost:8080

redis:
  balance_db: 0
  password: 12345
  addr: localhost:6379

mysql:
  username: root
  password: root
  db_name: im
  host: localhost
  port: 3306
  charset: utf8mb4
  zone: Asia%2FShanghai
  max_open_conns: 100
  max_idle_conns: 5

